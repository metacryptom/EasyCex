import { ExchageType, MarketFetcher, Pair } from '../base'

export function createMarket (pair:Pair, exchangeType :ExchageType) : MarketFetcher {
  switch (exchangeType) {
    case ExchageType.Okex:
    default:
  }
  return null
}
